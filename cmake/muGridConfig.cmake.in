@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Eigen3 3.4.0)

# Optional dependencies based on build configuration
set(MUGRID_ENABLE_MPI @MUGRID_ENABLE_MPI@)
set(MUGRID_ENABLE_NETCDF @MUGRID_ENABLE_NETCDF@)

if(MUGRID_ENABLE_MPI)
    find_dependency(MPI)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/muGridTargets.cmake")

check_required_components(muGrid)
