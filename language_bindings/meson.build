# We need this, otherwise the path to Python.h is not include
python3_dependency = python.dependency()

pybind11_incdir = include_directories('../external/pybind11/include')

subdir('libmugrid')
subdir('libmufft')
subdir('python')